import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: [
      "randomuser.me",
      "images.unsplash.com",
      "images.pexels.com",
      "www.pexels.com",
      "images.pexels.com",
      "cdn.brandfetch.io",
      "asset.brandfetch.io"

    ],
  },
  /* config options here */
};

export default nextConfig;
